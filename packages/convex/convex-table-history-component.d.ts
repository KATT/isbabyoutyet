declare module "convex-table-history/_generated/component.js" {
  // Older component packages do not export this path; Confect's components
  // registry still references it for typing. Runtime uses componentsGeneric().
  export type ComponentApi<_Name extends string = string> = any;
}
