# Local development defaults, loaded by Vite in dev mode only (no secrets).
# Production/preview values are provided as real environment variables by the
# deploy pipeline and always take precedence.
VITE_CONVEX_URL=http://127.0.0.1:3210
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211
VITE_SITE_URL=http://localhost:3000
